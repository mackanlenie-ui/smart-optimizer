name: Build Smart Gallery APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: [ 'smart-gallery/**', '.github/workflows/smart-gallery.yml' ]
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
   - uses: actions/setup-java@v4
     with: { distribution: temurin, java-version: '17' }
   - uses: android-actions/setup-android@v3
   - uses: gradle/actions/setup-gradle@v4
     with: { gradle-version: '8.9' }
   - name: Build APK
     working-directory: smart-gallery
     run: gradle --no-daemon :app:assembleDebug
   - name: Prepare
     run: |
       cp smart-gallery/app/build/outputs/apk/debug/app-debug.apk Smart-Gallery-1.0-unsigned.apk
       BT=$(ls -d "$ANDROID_HOME"/build-tools/* | sort -V | tail -1)
       mkdir -p apksigner-tool/lib
       cp "$BT/apksigner" apksigner-tool/
       cp "$BT/lib/apksigner.jar" apksigner-tool/lib/
   - uses: actions/upload-artifact@v4
     with: { name: Smart-Gallery-1.0-unsigned, path: Smart-Gallery-1.0-unsigned.apk }
   - uses: actions/upload-artifact@v4
     with: { name: smart-gallery-apksigner, path: apksigner-tool }
